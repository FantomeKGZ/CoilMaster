# Текущее состояние CoilMaster

Дата обновления: **2026-08-06**  
Ветка: **`cmp-protocol-v1`**  
Статус: **активная разработка**

## Назначение проекта

CoilMaster — система управления намоточным станком и мастерской по ремонту электродвигателей.

Проект объединяет:

- Arduino Uno как контроллер реального времени и безопасности;
- ESP32 как сервисный контроллер, веб-сервер и хранилище;
- UART-протокол между платами;
- реестр клиентов, двигателей и ремонтов;
- склад провода и дополнительных материалов;
- списание материалов на ремонт;
- калькуляцию себестоимости;
- мобильный и настольный веб-интерфейс;
- журнал фактических запусков намотки.

## Оценка готовности

Для первой реально используемой версии станка готовность оценивается примерно в **70–75%**.

Если включать расширенные будущие функции, такие как полноценные аналоги двигателей, неразобранные намотки и веб-генератор пересчёта алюминия на медь, общая готовность ближе к **60–65%**.

## Состояние основных подсистем

| Подсистема | Оценка | Состояние |
|---|---:|---|
| Аппаратные модули | 90% | Основные модули проверены пользователем |
| UART Arduino ↔ ESP32 | 85–90% | Обмен проверен, протокол активно усиливается |
| Безопасность запуска | 75–80% | Физический START обязателен, удалённое прямое включение SSR запрещено |
| Журнал намотки | 80% | Есть составная идентичность события, дедупликация и проверки переходов |
| Реестр клиентов/двигателей/ремонтов | 80% | Основные API и интерфейсы реализованы |
| Склад провода | 90% | CU/AL/UNKNOWN, списания, история, стоимость |
| Дополнительные материалы | 85–90% | Транзакции, восстановление, проверка ремонта и валюты |
| Калькуляция ремонта | 85% | Материалы и провод учитываются по сохранённой стоимости |
| Мобильный/настольный UI | 75% | Основные страницы работают, требуется финальная интеграция |
| Полный рабочий цикл намотки | 60–65% | Нужна окончательная привязка задания, запуска и ремонта |
| Комплексные испытания | 35–40% | Ещё нужны тесты отказов и длительные испытания |

## Что пользователь уже подтвердил

Пользователь сообщал о зелёных сборках и отсутствии ошибок после крупных этапов:

- склад провода и разделение CU/AL/UNKNOWN;
- стоимость списаний и округление;
- материалы ремонта и проверки ссылок на ремонт;
- строгий разбор UART-событий;
- ограниченные повторные отправки задания;
- защита непрочитанного результата доставки.

Последнее явное подтверждение перед текущими изменениями: **все зелёное** после защиты непрочитанного результата доставки.

## Текущая активная область

Работа сосредоточена на надёжности полного цикла намотки:

1. устойчивое выделение и хранение `session_id`;
2. неизменяемый снимок задания на ESP32;
3. доставка задания ESP32 → Arduino;
4. подтверждение или отклонение задания;
5. связь `job_id + session_id + run_id`;
6. физическое выполнение намотки;
7. последовательная история на SD;
8. защита от повторов, пересечений и старых идентификаторов.

## Последние фактические изменения

В актуальных `CM_WindingJournal.h/.cpp` уже реализована составная идентичность события:

```text
session_id + run_id + event_type
```

Методы `containsRunEvent()` и `hasRunStart()` используют точную пару `session_id/run_id`. Одинаковый числовой `run_id` в разных сессиях не должен ошибочно считаться дубликатом.

Документировано в:

```text
docs/80_COMPOSITE_WINDING_EVENT_IDENTITY.md
```

Коммит документа:

```text
7235f1e7e54e8603191685035e38eaeb333fef2e
```

Также создан контракт семантики `session_id`:

```text
docs/81_WINDING_SESSION_ID_SEMANTICS.md
```

Основное решение: `session_id` выделяет ESP32 для неизменяемого снимка задания; Arduino повторяет принятый идентификатор в событиях. Идентификатор не разрешает физический старт и должен сохраняться устойчиво.

Коммит документа:

```text
0aa21ae9b3f6ee0fe5871eb500846d99732e882b
```

### Статус проверки последних изменений

`NOT VERIFIED`.

Подключённый GitHub API не вернул workflow-runs/status checks для прямых коммитов. Поэтому нельзя утверждать, что текущий head зелёный, пока явно не проверены:

- `ESP32 Build`;
- `CMP Protocol Tests`.

Созданные в этой сессии изменения документационные и не меняют прошивку, но актуальный C++-код составной идентичности также должен быть подтверждён сборкой.

## Основные текущие риски

- Журнал событий читается линейным проходом по NDJSON; при очень большом журнале понадобится индекс или ротация.
- Устойчивый allocator `session_id` и неизменяемый job snapshot ещё не реализованы.
- `run_id` и `job_id` должны согласованно генерироваться и переживать перезапуск.
- Текущие события намотки содержат ограниченный набор полей и пока не несут полный снимок программы и ремонта.
- Нельзя автоматически списывать провод только по событию завершения без устойчивой привязки к ремонту и выбранной катушке.
- Некоторые старые документы или другая ветка могут описывать раннюю бинарную версию CMP; фактический код ветки использует строковые кадры `CMP1|...`.

## Что не считать реализованным

Пока не реализованы полностью:

- устойчивый allocator `session_id` на ESP32;
- неизменяемое хранилище снимков заданий;
- строгая связь `job_id/session_id/run_id/repair_id`;
- точное сравнение обмоток с учётом всех проводников;
- полноценные группы аналогов двигателей;
- автоматическая обработка неразобранных намоток;
- полный снимок программы намотки в журнале события;
- безопасное автоматическое продолжение незавершённой намотки после перезапуска;
- окончательные стресс-тесты на реальном станке.
