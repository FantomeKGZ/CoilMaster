name: Archived cash runtime patch

on:
  workflow_dispatch:

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Archived after successful guarded integration. No mutation is performed."
