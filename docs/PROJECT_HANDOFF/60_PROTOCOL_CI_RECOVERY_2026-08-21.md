# CoilMaster — CMP Protocol CI recovery

Date: 2026-08-21
Branch: `cmp-protocol-v1`

## Confirmed current baseline

`CMP Protocol Tests #2170` for commit `2c00b2c8d57e4f4dd0806ac29be7f24893ebf2c2` completed successfully in GitHub Actions.

The recovery sequence fixed stale regression-contract checks without weakening production safety semantics:

- Arduino archive tooltip markup is generated by the shared controller, while desktop HTML owns the tooltip styles.
- Mobile archive `<details>` markup is generated by the shared controller, while mobile HTML owns the details styles.
- Release SSR contract now reflects Arduino-local Hall calibration motor permit: `ssr.update(machine.state(), simulationMode(), hallCalibration.motorPermit())`.
- Release write-off contract now distinguishes legacy exact-spool semantics from manual KG_FIRST optional-spool semantics while retaining exact `source_session_id + source_run_id`, completion evidence, duplicate protection, and no automatic deduction.
- CMP workflow now executes all Web/safety audits with `if: always()` after the build/test stages so one stale contract does not hide later failures. Any failed audit still fails the job.
- KG-first warehouse persistence test no longer treats the legitimate spool-registry `findUnsigned(..., "spool_id", ...)` parser as a forbidden legacy movement parser; movement references remain codec-based and optional-spool aware.
- Write-off fault contract now matches escaped error tokens as they actually appear inside C++ JSON string literals.

## Confirmed passing in #2170

The run is green as a whole, therefore the current commit passed the workflow stages configured in `.github/workflows/cmp-protocol-tests.yml`, including host protocol/state-machine tests and all configured Web/safety audits.

## Safety invariants unchanged

- physical START only;
- no automatic physical START between repeats;
- no auto-resume after reboot;
- ESP32/Web do not directly control SSR;
- `RUN_COMPLETED` never performs automatic wire write-off;
- wire write-off remains explicit/manual with exact source-run provenance;
- restore remains operator-only and fail-closed.

## Next recovery priority

Protocol CI is no longer the blocker. Continue with the separately failing build workflows, starting with `ESP32 Build`, then verify `Arduino Uno Build` current HEAD and inspect any remaining compile/size regression. Do not infer hardware acceptance from CI.
