# CMP #4160–#4527 CI — 2026-08-30

Дата: **2026-08-30**  
Репозиторий: **`FantomeKGZ/CoilMaster`**  
Production/source-of-truth: **`cmp-protocol-v1`**  
Активная рабочая ветка: **`arduino-ru-lcd-experiment`**

## Exact verified CI

Documentation-only SUCCESS chain подтверждена continuously through **CMP #4524**. После этого начался новый functional audit/update block, поэтому #4525–#4527 фиксируются отдельно и не должны ошибочно описываться как непрерывная GREEN docs-chain.

Последний участок:

```text
CMP #4518  run 33312777122 / SUCCESS  head 0a9287c2ab4e22bf9236c7b742cc389993ae2158
CMP #4519  run 33312797865 / SUCCESS  head 626399bc84ac31cc791fc298376a7da666b2a85a
CMP #4520  run 33312873604 / SUCCESS  head b4510d4dca30b6d33be1d8c0d03087515b5e75a5
CMP #4521  run 33312892924 / SUCCESS  head 395d474732902650397c9ec53cb308d6e3c93b74
CMP #4522  run 33312914746 / SUCCESS  head 268383ac8d8ae12c03e952f2919f8a9c162d3e65
CMP #4523  run 33313015445 / SUCCESS  head 7eee9bc312bbaf07308e8213c9738cf8cda3202b
CMP #4524  run 33313040747 / SUCCESS  head 32da60826ee39e410afef0c30b69fc1e3fd63158
CMP #4525  run 33313307355 / FAILURE  head 4c6554a07b5e4ff8104ef0b9d8fc0914677ff9d5
CMP #4526  run 33313331225 / FAILURE  head da6b5423d782b73ed4ebacb9aaf5fa164d5ac552
CMP #4527  run 33313347671 / SUCCESS  head 1b7f8504184b681d5f7e0da7710c4a50601a346a
ESP32 #1780 run 33313307362 / SUCCESS head 4c6554a07b5e4ff8104ef0b9d8fc0914677ff9d5
```

#4523 verifies snapshot through #4522. #4524 verifies entrypoint through #4522. Transfer through #4522 had not yet been written when the functional audit started.

## Calculator functional audit / first reopened product item

The user requested a full audit of previously discussed additions/updates before moving on.

First concrete gap found: conductor calculator UI accepted several diameters but hard-coded every source component to `source_strands_N=1`, so the requested real-world input such as 0.80 mm x 3 strands / x 5 strands was not fully represented by the Web UI although backend already supported per-component strand counts 1..12.

Implemented:

```text
4c6554a07b5e4ff8104ef0b9d8fc0914677ff9d5  desktop source strand-count input
 da6b5423d782b73ed4ebacb9aaf5fa164d5ac552  mobile parity
1b7f8504184b681d5f7e0da7710c4a50601a346a  updated regression contract
```

Accepted input syntax now includes examples such as:

```text
0,80x3
1,00x5
0,80x3;1,00x2
```

Backend conversion semantics were not widened: existing validated `source_strands_N` range 1..12 is reused.

### Intermediate failures

CMP #4525 and #4526 are intentional/understood intermediate failures, not hidden regressions. Host configure/build/test and all preceding audits passed; the failing step was `Audit calculator source wire input`, whose old regression contract still required every entered diameter to map to exactly one source strand. The contract was then aligned to the newly intended UI behavior.

Recovery evidence:

```text
CMP #4527 run 33313347671 / SUCCESS
head 1b7f8504184b681d5f7e0da7710c4a50601a346a
```

ESP32 build evidence currently supplied:

```text
ESP32 #1780 run 33313307362 / SUCCESS
head 4c6554a07b5e4ff8104ef0b9d8fc0914677ff9d5
```

Do not infer an ESP32 GREEN result for later SHA unless an exact later ESP32 run is checked.

## Current engineering direction

Checkpoints 159–167 remain closed unless a concrete regression is found. The physical Arduino+ESP32 E2E remains operator-confirmed PASS for the previously tested hardware/firmware state.

A new **feature-completeness audit** is now active by explicit user request. It is not a speculative storage/performance sweep. Priority is to verify previously discussed product/UI additions against real code and finish only proven gaps.

Next audit targets include:

- clients/motors/repairs pagination;
- motor import workflow;
- shared Web shell/navigation/search/recent/breadcrumbs/time/version/toast layer;
- FTP/Web recovery;
- Wi-Fi profiles/static IP/`coil.local`;
- backup/settings;
- desktop/mobile parity;
- stale/empty pages and links.

Production remains unchanged:

```text
cmp-protocol-v1 = 28c7917a906bc9b15736369e8986d0e0c354ab8c
```

## Safety/integrity invariants

Unchanged: no automatic physical START/repeat START; no auto-resume; Arduino owns SSR; ESP32/Web never directly controls SSR; `RUN_COMPLETED` never auto-writes off wire; RUN_WIRE remains explicit/manual with exact `spool_id + source_session_id + source_run_id`; authoritative rereads/TOCTOU guards remain; append-only history is not silently edited/deleted; no automatic production truncation/deletion; no premature DB/index migration.

## Continuation

Latest exact CMP SUCCESS for the functional calculator block before this documentation update is:

```text
1b7f8504184b681d5f7e0da7710c4a50601a346a
CMP #4527 run 33313347671 / SUCCESS
```

Continue the feature-completeness audit from fresh `arduino-ru-lcd-experiment` HEAD. Do not return to documentation-only CI recursion as the main work stream.