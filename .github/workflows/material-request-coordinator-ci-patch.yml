name: One-shot material request coordinator CI patch

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - name: Archived one-shot workflow
        run: echo "Coordinator regression is wired permanently in cmp-protocol-tests.yml; this workflow is archived and read-only."
