name: Hall calibration foundation verifier retired

on:
  workflow_dispatch:

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Retired verifier; no source changes are performed.'
