# CoilMaster — pause handoff 2026-08-08 22:22 +06

Репозиторий: `FantomeKGZ/CoilMaster`  
Рабочая ветка: `cmp-protocol-v1`  
`main` не использовать как источник кода.

Этот файл создан как точка безопасной паузы. При следующем продолжении читать его первым, затем `06_ACTIVE_WORK_AND_NEXT_STEPS.md`, `01_CURRENT_STATE.md`, `09_KEY_FILES_INDEX.md` и только после этого заново fetch актуальные исходники из `cmp-protocol-v1`.

## Кодовый checkpoint перед handoff-коммитами

Последний подтверждённый кодовый HEAD перед записью этой паузы:

```text
ef0e64838ebb3f0519f6bfe756ade599a07450b9  Require exact run provenance in writeoff UI
```

После этого pause-файла HEAD ветки будет выше только за счёт документационных handoff-коммитов, если параллельная работа не внесёт кодовые изменения.

## Оценка готовности

Ориентировочно:

- реализованная функциональность production flow: **около 96%**;
- repository/firmware readiness: **около 93%**;
- общая эксплуатационная готовность: **около 90%**.

Последний показатель намеренно ниже из-за отсутствия подтверждённого GREEN Actions result для текущего head, полного ESP32 + Arduino hardware E2E и реального Stage 0 benchmark на production dataset.

## Production flow

```text
client → motor → OPEN repair → costing → linked winding → exact spool_id
→ immutable snapshot + spool-selection → UART → physical START
→ RUN_STARTED → RUN_COMPLETED
→ manual exact-run wire writeoff
→ materials/pricing → finalization preflight
→ CLOSED → archive/report → read-only backup
```

Safety invariants не менять:

- physical START только физический;
- ESP32/Web не управляют SSR напрямую;
- automatic resume после reboot отсутствует;
- `RUN_COMPLETED` сам по себе не списывает провод;
- новое списание провода только ручное;
- новый writeoff обязан иметь exact `spool_id + source_session_id + source_run_id`;
- corruption/storage loss блокирует опасную операцию fail-closed.

## Что закрыто в последнем блоке

### 1. Production web bootstrap

Текущий production bootstrap реально поднимает:

```text
workshop clients/motors/repairs
motor similarity
warehouse summary/spools/price
warehouse writeoff GET/POST
materials CRUD/usage/adjustments
repair costing/pricing history
conductor calculator/settings
backup manifest/file/session export
winding history
```

Ключевые commits:

```text
961229e410b91b788984a2ee14a1a8b4f1276073  Register warehouse writeoff routes in production bootstrap
74f5bea9273ab47d5df1c4ef2f0557445b21a6ed  Register production material and conductor APIs
75207162c7164121252057391230077d5340be3a  Remove duplicate winding history bootstrap
```

`/api/winding-history` должен регистрироваться **один раз** через `StaticSiteServer::begin()`. Не возвращать дополнительную регистрацию в `WarehouseWeb::begin()`.

### 2. Exact-run wire writeoff

Новые writeoff больше не допускаются без exact run provenance.

Storage boundary:

```text
c7335631c660a7b5ee71da880a1f77e4e5faa83f  Require exact run provenance for new wire writeoffs
```

`WarehouseStore::confirmSpoolWriteOff()` требует non-zero:

```text
source_session_id
source_run_id
```

и повторно доказывает `RUN_COMPLETED` + отсутствие уже подтверждённого списания этого run.

HTTP boundary:

```text
7b92010294342d2a9cc9a153f306673f5c66ffb9  Require run provenance in wire writeoff API
```

`POST /api/warehouse/write-offs` теперь требует оба поля; отсутствие provenance даёт `400 source_session_and_run_required`.

UI boundary:

```text
ef0e64838ebb3f0519f6bfe756ade599a07450b9  Require exact run provenance in writeoff UI
```

`firmware/esp32/web/shared/writeoff-spool-suggestion.js`:

- читает paginated `/api/winding-history?repair_id=...`;
- ищет завершённый run, который ещё не покрыт writeoff;
- сверяет immutable spool-selection;
- добавляет exact session/run только если выбран именно immutable spool;
- блокирует POST без доказанного provenance;
- после успешного ручного списания ищет следующий незакрытый completed run;
- не выполняет автоматическое списание.

Legacy session-only writeoff остаётся только read-only/grandfathered compatibility для старых данных и CLOSED coverage; **новые** операции session-only не создаются.

### 3. Warehouse repair reference

Tri-state overload восстановлен и strict:

```text
79876ce6...  Restore tri-state warehouse repair lookup
```

`WarehouseStore::repairExists(uint32_t, bool&)` выполняет full fail-closed scan и отличает `not found` от read/integrity failure. Bool overload делегирует ему.

### 4. Winding authority / startup / finalization

Закрыто ранее в этом же проходе:

```text
f3e02dadce34c7a85e11d23154652a469d6aa111  strict validateAll JSON
d92b6f1c19a5d0c5c2abb3663410e9202bcd385e  finalization uses authoritative validateAll
9e18cd076b8e1ad904e3893aab20cde1a4e596a8  writeoff requires winding integrity
6a9d47f8...  transition audit strict JSON
d7c601fe18d8b60794334cb2572e40a7777cc0d2  startup journal strict JSON
```

### 5. Flat JSON persistence hardening

Shared validator:

```text
firmware/esp32/src/CM_FlatJsonObjectValidator.h
```

Workshop, pricing, material, warehouse, settings and winding flat persisted readers/audits усилены fail-closed без telemetry-only filesystem scans.

Крупный `CM_MaterialLedger.cpp` также закрыт:

```text
c4ff5ff4d195959c3fc70bd8e0b8bf72ac5a0525  Fail closed on malformed material ledger runtime state
```

### 6. Conductor settings

Production authoritative file:

```text
/data/settings/conductor.json
```

Не возвращаться к experimental `/data/settings/conductor-calculator.ndjson` как production source.

Закрыто:

```text
6cde2831f190918f102ca39eb9f6b3687a7602ea  persist allow_mixed_diameters
932624e5a83187eca6545182fefb3cfd81f0bc77  audit production conductor settings
012cd0816a53489bb6169b69f3079f6ce46f9de5  backup exports production conductor settings
ce61bbbf...  reject invalid allow_mixed_diameters input
```

## Stage 0 backup observability

Не расширять ради количества: текущих **29 metrics** достаточно для стенда.

Снять на hardware E2E:

```text
items[].size_bytes
snapshot_stability_duration_ms
9 per-domain *_duration_ms
allocator high-water
material/business/winding/warehouse record counts
winding session file counts + total bytes
```

После benchmark выбирать первый измеренный hotspot. До этого не вводить arbitrary rotation threshold, persistent optimistic cache или DB migration.

## CI / build verification

Исторически подтверждённая failure:

```text
CM_MaterialLedger.cpp: expected '}' at end of input
```

Исправление:

```text
77fd7dd4db3767c33106d63e6f9174e6559b9bc8
```

Для текущего head connector не показал combined statuses/PR workflow runs. Поэтому статус: **CI NOT CONFIRMED**, не GREEN.

Workflow `esp32-build.yml` настроен на push в `cmp-protocol-v1`, но connector не показывает общий push-run список.

## Точная следующая repo-only точка

1. Сначала проверить актуальный HEAD и Actions/build, если run становится доступен.
2. Проверить compile/link availability `WarehouseStore::confirmedWriteOffForSourceRun(...)` и `confirmedWriteOffForSourceSession(...)` через точный branch path/history или реальный build. **Не создавать второй symbol по догадке.**
3. Если symbol определён и build чистый — не продолжать бесконечный parser-hardening; перейти к hardware E2E/benchmark.
4. Performance refactor `WireWriteOffCoverageAudit` и repeated NDJSON scans делать только после измерений либо отдельной correctness-причины.

## Обязательный внешний этап

Полный ESP32 + Arduino E2E:

```text
linked repair
→ exact spool
→ JOB_ACK
→ physical START
→ RUN_STARTED
→ RUN_COMPLETED
→ manual exact-run wire writeoff
→ costing
→ finalization preflight
→ CLOSED
→ stable backup
```

Fault cases:

- reboot/manual-review;
- microSD loss;
- corrupted persistence;
- UART timeout/reject/duplicate events;
- duplicate writeoff;
- missing/wrong session/run/spool;
- close without wire coverage;
- backup during active winding.

## Правило продолжения

Перед каждым изменением существующего файла:

1. fetch актуальную версию именно из `cmp-protocol-v1`;
2. использовать текущий blob SHA;
3. при `409` re-fetch + merge, не перетирать concurrent work.

Для нового файла сначала проверять, что пути нет.

Не ждать CI, если connector его не видит, и не называть head GREEN без фактического результата.
