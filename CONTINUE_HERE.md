# Continue CoilMaster Here

Этот файл — единая точка входа после перерыва или при переходе в новый чат.

## Единственный актуальный handoff

Рабочая ветка:

```text
cmp-protocol-v1
```

Единственный актуальный каталог контекста:

```text
docs/PROJECT_HANDOFF/
```

Старый каталог `Docs/AI_CONTEXT/` удалён и больше не используется.

## Порядок продолжения

1. Переключиться на ветку `cmp-protocol-v1`.
2. Открыть `docs/PROJECT_HANDOFF/00_READ_FIRST.md`.
3. Прочитать остальные файлы каталога в указанном там порядке.
4. Проверить актуальные версии исходников и последние коммиты.
5. Продолжить с точки из `docs/PROJECT_HANDOFF/06_ACTIVE_WORK_AND_NEXT_STEPS.md`.
6. После значимой работы обновить минимум файлы `01`, `05` и `06`.

## Запрос для нового чата

> Открой репозиторий `FantomeKGZ/CoilMaster`, выбери ветку `cmp-protocol-v1`, прочитай все файлы каталога `docs/PROJECT_HANDOFF/`, начиная с `00_READ_FIRST.md`, проверь актуальные версии исходников и продолжи с точки из `06_ACTIVE_WORK_AND_NEXT_STEPS.md`. В конце обнови handoff-файлы.
