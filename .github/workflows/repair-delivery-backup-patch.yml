name: Archived one-shot repair delivery backup patch

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Archived one-shot workflow; no repository mutation."
