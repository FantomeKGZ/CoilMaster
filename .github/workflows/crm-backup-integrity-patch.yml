name: Archived CRM backup integrity patch

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - name: Archived one-shot workflow
        run: echo "CRM backup integrity patch already applied; this workflow is intentionally inert."
