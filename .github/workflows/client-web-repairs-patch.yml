name: Archived Client Web Repair Helper

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Archived guarded helper; no repository mutations."
