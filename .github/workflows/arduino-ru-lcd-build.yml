name: Arduino RU LCD Build

on:
  workflow_dispatch:
  push:
    branches:
      - arduino-ru-lcd-experiment
    paths:
      - "firmware/arduino/**"
      - "firmware/esp32/src/**"
      - "firmware/esp32/web/**"
      - "Arduino/**"
      - "Core/**"
      - "Shared/**"
      - "Tests/Web/check_ru_hall_calibration_experiment.js"
      - "Tests/Web/check_motor_edit_ui.js"
      - "platformio.ini"
      - ".github/workflows/arduino-ru-lcd-build.yml"

jobs:
  compare-builds:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Audit RU Hall calibration experiment contracts
        run: node Tests/Web/check_ru_hall_calibration_experiment.js

      - name: Audit editable motor card contracts
        run: node Tests/Web/check_motor_edit_ui.js

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.x"

      - name: Install PlatformIO
        run: pip install --upgrade platformio

      - name: Build normal Uno firmware
        shell: bash
        run: |
          set -o pipefail
          pio run -e uno | tee uno-build.log

      - name: Build RU EN Uno firmware
        shell: bash
        run: |
          set -o pipefail
          pio run -e uno_ru | tee uno-ru-build.log

      - name: Build ESP32 firmware
        shell: bash
        run: |
          set -o pipefail
          pio run -e esp32 | tee esp32-build.log

      - name: Compare resource usage
        run: |
          python - <<'PY'
          import re
          from pathlib import Path

          def usage(path, label):
              text = Path(path).read_text(encoding='utf-8', errors='replace')
              line = next((line for line in text.splitlines() if f'{label}:' in line), None)
              if line is None:
                  raise SystemExit(f'{path}: {label} usage line missing')
              match = re.search(r'used\s+(\d+)\s+bytes\s+from\s+(\d+)\s+bytes', line)
              if match is None:
                  raise SystemExit(f'{path}: {label} usage numbers missing: {line}')
              return tuple(map(int, match.groups()))

          print('Arduino Uno RU/EN resource comparison')
          for label in ('RAM', 'Flash'):
              normal_used, total = usage('uno-build.log', label)
              ru_used, ru_total = usage('uno-ru-build.log', label)
              if total != ru_total:
                  raise SystemExit(f'{label}: target totals differ')
              delta = ru_used - normal_used
              print(
                  f'{label}: normal={normal_used} RU_EN={ru_used} '
                  f'delta={delta:+d} headroom={total - ru_used} total={total}'
              )
          PY

      - name: Report largest RU firmware symbols
        shell: bash
        run: |
          NM="$HOME/.platformio/packages/toolchain-atmelavr/bin/avr-nm"
          echo "Largest uno_ru symbols (decimal bytes):"
          "$NM" -C --defined-only --print-size --size-sort --radix=d \
            .pio/build/uno_ru/firmware.elf | tail -n 50
