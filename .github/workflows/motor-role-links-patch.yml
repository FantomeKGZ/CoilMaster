name: Archived Motor Role Links Patch

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - run: echo "One-shot patch already applied; no repository mutation is performed."
