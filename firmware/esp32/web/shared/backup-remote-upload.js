(()=>{let timer=0;const manifestState=document.getElementById('state');if(!manifestState)return;const status=document.createElement('div');status.id='remoteBackupState';status.className='muted';status.style.marginTop='10px';status.textContent='FTP: ожидание запуска.';manifestState.parentNode.insertBefore(status,manifestState.nextSibling);const size=n=>{const v=Number(n)||0;return v<1024?v+' Б':(v/1024).toFixed(1)+' КБ'};function enhance(){document.querySelectorAll('a[href^="/api/backup/file?name="]').forEach(a=>{if(a.dataset.remoteReady)return;a.dataset.remoteReady='1';const name=new URL(a.href,location.href).searchParams.get('name');if(!name)return;const b=document.createElement('button');b.type='button';b.dataset.remoteBackup=name;b.textContent='На сервер';a.insertAdjacentElement('afterend',b)})}function buttons(disabled){document.querySelectorAll('[data-remote-backup]').forEach(b=>b.disabled=disabled)}async function poll(){enhance();try{const r=await fetch('/api/backup/remote/status',{cache:'no-store'}),j=await r.json();if(!r.ok)throw new Error(j.error||'status_failed');if(j.active){status.className='muted';status.textContent='FTP: '+j.state+' · '+size(j.bytes_sent)+' из '+size(j.bytes_total);buttons(true);timer=setTimeout(poll,700);return}buttons(false);if(j.succeeded){status.className='ok';status.textContent='FTP: файл '+j.name+' передан, проверен и переименован.'}else if(j.state==='FAILED'){status.className='bad';status.textContent='FTP: ошибка '+(j.error||'unknown')+'. На сервере может остаться только .part.'}else status.textContent='FTP: ожидание запуска.'}catch(e){buttons(false);status.className='bad';status.textContent='FTP status недоступен: '+e.message}}async function upload(name){clearTimeout(timer);buttons(true);status.className='muted';status.textContent='FTP: запуск передачи…';try{const body=new URLSearchParams({name});const r=await fetch('/api/backup/remote/upload',{method:'POST',headers:{'Content-Type':'application/x-www-form-urlencoded'},body}),j=await r.json();if(!r.ok)throw new Error(j.reason||j.error||'start_failed');poll()}catch(e){buttons(false);status.className='bad';status.textContent='FTP: запуск отклонён — '+e.message}}document.addEventListener('click',e=>{const b=e.target.closest('[data-remote-backup]');if(b)upload(b.dataset.remoteBackup)});new MutationObserver(enhance).observe(document.body,{childList:true,subtree:true});poll()})();
