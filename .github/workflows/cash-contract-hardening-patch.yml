name: Archived cash contract hardening patch

on:
  workflow_dispatch:

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Archived before execution; hardening is implemented in normal source files."
